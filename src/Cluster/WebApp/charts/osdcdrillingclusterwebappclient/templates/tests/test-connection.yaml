apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingclusterwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingclusterwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingclusterwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
