apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingclusterservice.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingclusterservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingclusterservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
