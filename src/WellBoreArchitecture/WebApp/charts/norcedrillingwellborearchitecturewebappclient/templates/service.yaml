apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingwellborearchitecturewebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingwellborearchitecturewebappclient.labels" . | nindent 4 }}  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingwellborearchitecturewebappclient.selectorLabels" . | nindent 4 }}

