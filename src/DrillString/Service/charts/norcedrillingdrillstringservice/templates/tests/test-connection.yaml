apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingdrillstringservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingdrillstringservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingdrillstringservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
