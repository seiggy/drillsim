apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingdrillstringwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingdrillstringwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingdrillstringwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
