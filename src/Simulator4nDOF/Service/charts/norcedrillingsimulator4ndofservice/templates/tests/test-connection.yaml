apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingsimulator4ndofservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingsimulator4ndofservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingsimulator4ndofservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
