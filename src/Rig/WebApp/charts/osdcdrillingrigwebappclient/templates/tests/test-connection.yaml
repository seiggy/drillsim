apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingrigwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingrigwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingrigwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
