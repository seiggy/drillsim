apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingrigservice.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingrigservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingrigservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
