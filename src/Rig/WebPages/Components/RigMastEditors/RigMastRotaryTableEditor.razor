@namespace OSDC.Drilling.Rig.WebPages.Components.RigMastEditors

<RigComplexPropertyEditor Title="Rotary Table"
                          Value="RigMast.RotaryTable"
                          ValueChanged="@(value => RigMast.RotaryTable = (RotaryTable?)value)"
                          ModelType="@typeof(RotaryTable)" />

@code {
    [Parameter, EditorRequired] public RigMast RigMast { get; set; } = default!;
}

