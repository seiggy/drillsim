@namespace OSDC.Drilling.Rig.WebPages.Components.RigMastEditors

<RigComplexPropertyEditor Title="Torque Turn Sub"
                          Value="RigMast.TorqueTurnSub"
                          ValueChanged="@(value => RigMast.TorqueTurnSub = (TorqueTurnSub?)value)"
                          ModelType="@typeof(TorqueTurnSub)" />

@code {
    [Parameter, EditorRequired] public RigMast RigMast { get; set; } = default!;
}

