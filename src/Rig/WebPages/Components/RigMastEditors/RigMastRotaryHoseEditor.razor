@namespace OSDC.Drilling.Rig.WebPages.Components.RigMastEditors

<RigComplexPropertyEditor Title="Rotary Hose"
                          Value="RigMast.RotaryHose"
                          ValueChanged="@(value => RigMast.RotaryHose = (RotaryHose?)value)"
                          ModelType="@typeof(RotaryHose)" />

@code {
    [Parameter, EditorRequired] public RigMast RigMast { get; set; } = default!;
}

