@namespace OSDC.Drilling.Rig.WebPages.Components.RigMastEditors

<RigComplexPropertyEditor Title="Stand Pipe"
                          Value="RigMast.StandPipe"
                          ValueChanged="@(value => RigMast.StandPipe = (StandPipe?)value)"
                          ModelType="@typeof(StandPipe)" />

@code {
    [Parameter, EditorRequired] public RigMast RigMast { get; set; } = default!;
}

