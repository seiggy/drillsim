@namespace OSDC.Drilling.Rig.WebPages.Components.RigMastEditors

<RigComplexPropertyEditor Title="Cat Walk"
                          Value="RigMast.CatWalk"
                          ValueChanged="@(value => RigMast.CatWalk = (CatWalk?)value)"
                          ModelType="@typeof(CatWalk)" />

@code {
    [Parameter, EditorRequired] public RigMast RigMast { get; set; } = default!;
}

