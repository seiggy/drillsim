@namespace OSDC.Drilling.Rig.WebPages.Components.RigMastEditors

<RigComplexPropertyEditor Title="Top Drive"
                          Value="RigMast.TopDrive"
                          ValueChanged="@(value => RigMast.TopDrive = (TopDrive?)value)"
                          ModelType="@typeof(TopDrive)" />

@code {
    [Parameter, EditorRequired] public RigMast RigMast { get; set; } = default!;
}

