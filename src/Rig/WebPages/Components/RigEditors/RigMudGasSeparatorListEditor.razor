@using System.Collections
@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigListPropertyEditor Title="Mud Gas Separator List"
                       Items="(IList?)Rig.MudGasSeparatorList"
                       ItemsChanged="@(items => Rig.MudGasSeparatorList = items.Cast<MudGasSeparator>().ToList())"
                       ItemType="@typeof(MudGasSeparator)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

