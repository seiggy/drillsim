@using System.Collections
@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigListPropertyEditor Title="Desander List"
                       Items="(IList?)Rig.DesanderList"
                       ItemsChanged="@(items => Rig.DesanderList = items.Cast<Desander>().ToList())"
                       ItemType="@typeof(Desander)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

