@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigComplexPropertyEditor Title="Auto Driller"
                          Value="Rig.AutoDriller"
                          ValueChanged="@(value => Rig.AutoDriller = (AutoDriller?)value)"
                          ModelType="@typeof(AutoDriller)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

