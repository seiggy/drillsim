@using System.Collections
@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigListPropertyEditor Title="Desilter List"
                       Items="(IList?)Rig.DesilterList"
                       ItemsChanged="@(items => Rig.DesilterList = items.Cast<Desilter>().ToList())"
                       ItemType="@typeof(Desilter)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

