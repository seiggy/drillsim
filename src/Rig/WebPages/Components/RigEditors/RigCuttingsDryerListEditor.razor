@using System.Collections
@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigListPropertyEditor Title="Cuttings Dryer List"
                       Items="(IList?)Rig.CuttingsDryerList"
                       ItemsChanged="@(items => Rig.CuttingsDryerList = items.Cast<CuttingsDryer>().ToList())"
                       ItemType="@typeof(CuttingsDryer)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

