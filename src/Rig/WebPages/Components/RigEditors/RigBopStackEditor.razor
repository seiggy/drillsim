@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigComplexPropertyEditor Title="BOP Stack"
                          Value="Rig.BopStack"
                          ValueChanged="@(value => Rig.BopStack = (BopStack?)value)"
                          ModelType="@typeof(BopStack)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

