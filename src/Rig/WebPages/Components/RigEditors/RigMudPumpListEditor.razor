@using System.Collections
@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigListPropertyEditor Title="Mud Pump List"
                       Items="(IList?)Rig.MudPumpList"
                       ItemsChanged="@(items => Rig.MudPumpList = items.Cast<MudPump>().ToList())"
                       ItemType="@typeof(MudPump)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

