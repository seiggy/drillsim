@namespace OSDC.Drilling.Rig.WebPages.Components.RigEditors

<RigComplexPropertyEditor Title="Surface MPD Equipment"
                          Value="Rig.SurfaceMpdEquipment"
                          ValueChanged="@(value => Rig.SurfaceMpdEquipment = (SurfaceMpdEquipment?)value)"
                          ModelType="@typeof(SurfaceMpdEquipment)" />

@code {
    [Parameter, EditorRequired] public Rig Rig { get; set; } = default!;
}

