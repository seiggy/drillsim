apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingwellboreservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingwellboreservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingwellboreservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
