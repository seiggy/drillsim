apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingwellborewebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingwellborewebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingwellborewebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
