apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillinggeologicalpropertiesservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillinggeologicalpropertiesservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillinggeologicalpropertiesservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
