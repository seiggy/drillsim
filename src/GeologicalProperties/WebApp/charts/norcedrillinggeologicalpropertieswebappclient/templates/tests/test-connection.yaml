apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillinggeologicalpropertieswebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillinggeologicalpropertieswebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillinggeologicalpropertieswebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
