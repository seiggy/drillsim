apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingtrajectorywebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingtrajectorywebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingtrajectorywebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
