apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "earthverticaldatumservice.fullname" . }}
  labels: {{- include "earthverticaldatumservice.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels: {{- include "earthverticaldatumservice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations: {{- toYaml .Values.podAnnotations | nindent 8 }}
      labels:
        {{- include "earthverticaldatumservice.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}{{ toYaml . | nindent 8 }}{{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "earthverticaldatumservice.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - { name: ASPNETCORE_URLS, value: "http://+:8080" }
            - { name: EarthVerticalDatum__MaximumPositionsPerRequest, value: {{ .Values.earthVerticalDatum.maximumPositionsPerRequest | quote }} }
          ports:
            - { name: http, containerPort: 8080 }
          {{- if .Values.probes.enabled }}
          readinessProbe:
            httpGet: { path: /EarthVerticalDatum/api/health/ready, port: http }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /EarthVerticalDatum/api/health/live, port: http }
            initialDelaySeconds: 10
          {{- end }}
          resources: {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
