apiVersion: v1
kind: Service
metadata:
  name: {{ include "earthverticaldatumwebapp.fullname" . }}
  labels: {{- include "earthverticaldatumwebapp.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: {{ .Values.service.sessionAffinity }}
  selector: {{- include "earthverticaldatumwebapp.selectorLabels" . | nindent 4 }}
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: http }
