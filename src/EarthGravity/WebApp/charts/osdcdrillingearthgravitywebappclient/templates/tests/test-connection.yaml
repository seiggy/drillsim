apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "earthgravitywebapp.fullname" . }}-test-connection"
  labels:
    {{- include "earthgravitywebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "earthgravitywebapp.fullname" . }}:{{ .Values.service.port }}/EarthGravity/webapp/health/live"]
  restartPolicy: Never
