apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingdrillingfluidwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingdrillingfluidwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingdrillingfluidwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
