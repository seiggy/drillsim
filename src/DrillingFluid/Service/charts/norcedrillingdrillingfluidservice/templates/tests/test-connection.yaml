apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingdrillingfluidservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingdrillingfluidservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingdrillingfluidservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
