apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingfieldservice.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingfieldservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingfieldservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
