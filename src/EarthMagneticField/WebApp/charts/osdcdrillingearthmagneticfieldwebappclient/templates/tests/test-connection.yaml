apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "earthmagneticfieldwebapp.fullname" . }}-test-connection"
  labels:
    {{- include "earthmagneticfieldwebapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args: ["{{ include "earthmagneticfieldwebapp.fullname" . }}:{{ .Values.service.port }}/EarthMagneticField/webapp/health/live"]
  restartPolicy: Never
