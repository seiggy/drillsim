apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillinggeodeticdatumwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillinggeodeticdatumwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillinggeodeticdatumwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
