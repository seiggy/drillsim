apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingwellservice.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingwellservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingwellservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
