apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingwellwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingwellwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingwellwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
