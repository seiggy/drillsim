apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillinggeothermalpropertieswebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillinggeothermalpropertieswebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillinggeothermalpropertieswebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
