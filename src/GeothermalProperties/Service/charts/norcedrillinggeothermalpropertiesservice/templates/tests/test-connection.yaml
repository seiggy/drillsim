apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillinggeothermalpropertiesservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillinggeothermalpropertiesservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillinggeothermalpropertiesservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
