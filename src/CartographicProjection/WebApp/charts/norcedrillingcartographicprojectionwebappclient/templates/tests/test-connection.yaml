apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingcartographicprojectionwebappclient.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingcartographicprojectionwebappclient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingcartographicprojectionwebappclient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
