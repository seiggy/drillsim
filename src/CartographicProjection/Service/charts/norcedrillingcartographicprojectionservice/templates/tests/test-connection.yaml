apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "norcedrillingcartographicprojectionservice.fullname" . }}-test-connection"
  labels:
    {{- include "norcedrillingcartographicprojectionservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "norcedrillingcartographicprojectionservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
